import { render, screen } from '@testing-library/react';
import App from './App';

test('renders learn react link', () => {
  // Commented out to make sure this test module runs, otherwise will default to running Fib.js
  // which will trigger an error since services are not yet running
  // render(<App />);
  // const linkElement = screen.getByText(/learn react/i);
  // expect(linkElement).toBeInTheDocument();
});
